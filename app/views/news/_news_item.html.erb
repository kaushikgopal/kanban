<% if !object.blank? %> 
<div class="news_item">
	<div class="metadata">
		<div class="profile_info"><%= object.user.username %></div>
		<div class="datetime">
			<div class="date"><%= object.created_at.strftime('%d %b %Y') %></div>
			<div class="time"><%= object.created_at.strftime('%I:%M %p')  %></div>

		</div>
	</div>
	<div class="content"><%= object.content %></div>
</div>
<% end %>