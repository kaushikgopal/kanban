// alert("ajax works");
$('.delete_news_item')
	.bind('ajax:success', function() {$(this).closest('.news_item').fadeOut();})
	.bind('ajax:failure', function() {alert('Sorry, something went wrong. Please try again or refresh the page.')}); 